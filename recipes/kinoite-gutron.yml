---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

name: kinoite-gutron
description: Custom Fedora Kinoite image with personal tweaks - configured for hardware and use of an intel-based PC I use as a mini-server.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/blue-build/base-images/fedora-kinoite
image-version: 44

modules:
  - from-file: common/base.yml
  - from-file: common/va-intel.yml

  - type: os-release
    properties:
      DEFAULT_HOSTNAME: gutron

  - type: dnf
    install:
      packages:
        - glances
        - python3-jinja2
      
      # docker
        - docker
        - docker-compose

      # NAS
        - samba

  - type: files
    files:
      - source: gutron-root
        destination: /

  - type: script
    snippets:
      - "ls /usr/lib/firmware/edid"
      - "sudo chown root:root /usr/lib/firmware/edid/e214i-edid.bin"

  - type: kargs
    kargs:
      - drm.edid_firmware=DP-1:edid/e241i-edid.bin
      - video=DP-1:1920x1200@60e
