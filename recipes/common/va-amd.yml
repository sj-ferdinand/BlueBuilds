---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

modules:
  - type: dnf
    install:
      packages:
      # AMD hardware acceleration drivers
        - mesa-va-drivers-freeworld
        - mesa-vdpau-drivers-freeworld
